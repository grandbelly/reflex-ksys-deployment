
reflex==0.8.9
psycopg[binary,pool]>=3.1
asyncpg>=0.30.0
sqlalchemy>=2.0.0
pydantic>=2.6
cachetools>=5
reflex_chakra
plotly>=5.17.0

# AI/RAG 라이브러리 (유지)
sentence-transformers==2.2.2
numpy>=1.24.3
openai>=1.35.0

# 보안 라이브러리
cryptography>=41.0.0

# 파일 감시
watchdog>=3.0.0

# 직렬화 지원 (asyncio.Task 등 복잡한 객체)
dill>=0.3.8

# 데이터 분석 라이브러리 (경량화)
pandas>=2.0.0

# 시계열 예측 라이브러리 (통합 경량 라이브러리)
statsforecast>=1.7.0  # Unified lightweight library (AutoARIMA, ETS, etc.)
scikit-learn>=1.3.0  # For feature engineering, metrics
joblib>=1.3.0  # For model serialization

# 선택적 라이브러리 (필요시 추가)
xgboost>=2.0.0  # XGBoost ML model
prophet>=1.1.5  # Facebook Prophet for time series
# lightgbm>=4.0.0  # LightGBM (optional, faster than XGBoost)

# 테스팅 라이브러리
pytest>=8.0.0
pytest-asyncio>=0.24.0
